<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProtegePlus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:statusBarColor">@color/protegeRed</item>

        <item name="android:fontFamily">@font/nunito_sans_bold</item>
    </style>

    <style name="Theme.ProtegePlus" parent="Base.Theme.ProtegePlus" />
</resources>